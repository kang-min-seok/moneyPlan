import 'package:flutter/material.dart';

class MainWeek extends StatelessWidget {
  const MainWeek({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '주간 페이지',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
